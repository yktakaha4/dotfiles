sudo apt install -y \
  zsh \
  git \
  tmux \
  gh

if [ -n "$DOTFILES_INSTALL_DEV" ]; then
  sudo apt install -y shellcheck curl
  which shellspec >/dev/null || (
    curl -fsSL https://git.io/shellspec | sh -s -- --yes
  )
fi
